import { LightningElement, api } from 'lwc';

export default class RecordViewFormLDS extends LightningElement {
    // Hardcoded Account Id for record view on App/Home/Record pages
    @api recordId = '001dM00003EzdcLQAR';
}